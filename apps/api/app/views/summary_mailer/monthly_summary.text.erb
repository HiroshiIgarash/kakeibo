【月次レポート】<%= @target_month.year %>年<%= @target_month.month %>月の家計まとめ
================================================

■ 前月の総支出
<%= number_with_delimiter(@total_spent) %> 円

■ カテゴリ別支出
<% @category_totals.each do |category, amount| %>
<%= category.name %>: <%= number_with_delimiter(amount) %> 円
<% end %>

■ 来月の推奨予算（直近3ヶ月平均）
<% @recommended_budgets.each do |category, amount| %>
<%= category.name %>: <%= number_with_delimiter(amount) %> 円
<% end %>

================================================
このメールはかけいぼシステムから自動送信されています。
